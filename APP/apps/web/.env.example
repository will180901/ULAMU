# Comme l'app mobile (apps/mobile/src/config.ts) : on pointe directement sur l'API déjà déployée
# et sa base Neon — jamais de serveur local, jamais de Docker.
VITE_API_URL=https://ulamu-api.onrender.com
