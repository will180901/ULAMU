/**
 * Obsolète — l'OTP SMS est désormais intégré à l'étape 3 de RegisterScreen (inscription).
 * La 2FA de connexion est TotpChallengeScreen. Stub neutre, non référencé.
 */
import React from 'react';
import {View} from 'react-native';

export function OtpScreen(): React.JSX.Element {
  return <View />;
}
