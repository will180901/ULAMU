packages:
  - "apps/api"
  - "apps/desktop"
  - "packages/*"
  # apps/mobile est volontairement HORS du workspace pnpm : c'est une app React Native
  # autonome installée avec npm (flux RN standard, sans friction Metro/symlinks). Le socle
  # partagé y est vendoré dans src/lib/ (source de vérité : packages/contracts + packages/shared).
